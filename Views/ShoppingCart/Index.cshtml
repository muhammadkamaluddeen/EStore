@model ShoppingCartViewModel

<h2>Your shopping cart</h2>
<h4>Here are the products in your shopping cart.</h4>
<table class="table table-bordered table-striped">
    <thead>
        <tr>
            <th>Selected amount</th>
            <th>Product</th>
            <th class="text-right">Price</th>
            <th class="text-right">Subtotal</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var line in Model.ShoppingCart.ShoppingCartItems)
        {
<tr>
    <td class="text-center">@line.Amount</td>
    <td class="text-left">@line.Product.ProductName</td>
    <td class="text-right">@line.Product.Price.ToString("")</td>
    <td class="text-right">
        @((line.Amount * line.Product.Price).ToString(""))
    </td>
   <td><a asp-action="RemoveFromShoppingCart" asp-route-id="@line.Product.ProductId">Remove</a></td>
</tr>}
    </tbody>
    <tfoot>
        <tr>
            <td colspan="3" class="text-right">Total:</td>
            <td class="text-right">
                @Model.ShoppingCartTotal.ToString()
            </td>
        </tr>
    </tfoot>
</table>

<div class="text-center">
    <a class="btn btn-primary" asp-controller="Order" asp-action="Checkout">Check out now!</a>
</div>
